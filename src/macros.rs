// macro_rules! usage_and_exit {
//     ($app:ident) => {{
//         use std::process;
//         use std::io::prelude::*;
//         use std::io;

//         write!(io::stdout(), "{}", $app.usage()).expect("Could not write to stdout");
//         process::exit(1);
//     }};
// }
